editor = "vim"
notes_dir = "/tmp/cli-notes-test-dir"
templates_dir = "./test-config/templates"

[subcommands]
ls = "sh ./list-all-notes {{meta.list-flags}}™"
new-meeting = "create-meetings-binary {{setup.notes-dir}}"

# Meta get's merged with NewNote object and 
# can be accessed in template as for example {{meta.who}}
[meta]
author = "Max"
list-flags = "-la"
who = "Max Hill"
# timestamp = "1700766386383"
timestamp = "2023-01-02T18:00:00"

